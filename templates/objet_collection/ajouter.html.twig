{% extends 'base.html.twig' %}

{% block title %}Ajouter un {{ type }}{% endblock %}

{% block body %}
    <h1>Ajouter un {{ type }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('homepage') }}" class="btn btn-secondary mt-3">Annuler</a>
{% endblock %}