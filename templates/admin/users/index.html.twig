{% extends 'admin/base.html.twig' %}

{% block title %}Gestion des Utilisateurs{% endblock %}

{% block content %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Gestion des Utilisateurs</h1>
        <a href="{{ path('admin_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left me-2"></i> Retour au Dashboard
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead class="thead-light">
                <tr>
                    <th>ID</th>
                    <th>Email</th>
                    <th>Rôles</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for utilisateur in utilisateurs %}
                    <tr>
                        <td>{{ utilisateur.id }}</td>
                        <td>{{ utilisateur.email }}</td>
                        <td>
                            {% for role in utilisateur.roles %}
                                <span class="badge bg-secondary">{{ role }}</span>{% if not loop.last %}, {% endif %}
                            {% endfor %}
                        </td>
                        <td class="text-center">
                            <a href="{{ path('admin_users_edit', {'id': utilisateur.id}) }}" class="btn btn-sm btn-primary">
                                <i class="bi bi-pencil me-1"></i> Modifier
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center">Aucun utilisateur trouvé.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    {# Ajouter un bouton pour créer un nouvel utilisateur si vous avez la route #}
    {# <div class="mt-3">
    {#     <a href="{{ path('admin_users_new') }}" class="btn btn-success">
    {#         <i class="bi bi-person-plus me-1"></i> Ajouter un Utilisateur
    {#     </a>
    {# </div> #}
{% endblock %}